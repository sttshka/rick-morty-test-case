export enum StatusEnum {
  alive = 'Alive',
  dead = 'Dead',
  unknown = 'unknown'
}

export enum GenderEnum {
  female = 'Female',
  male = 'Male',
  genderless = 'Genderless',
  unknown = 'unknown'
}
